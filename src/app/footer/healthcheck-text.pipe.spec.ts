import { HealthcheckTextPipe } from './healthcheck-text.pipe';

describe('HealthcheckTextPipe', () => {
  it('create an instance', () => {
    const pipe = new HealthcheckTextPipe();
    expect(pipe).toBeTruthy();
  });
});
