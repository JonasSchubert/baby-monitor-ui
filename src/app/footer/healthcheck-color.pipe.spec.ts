import { HealthcheckColorPipe } from './healthcheck-color.pipe';

describe('HealthcheckColorPipe', () => {
  it('create an instance', () => {
    const pipe = new HealthcheckColorPipe();
    expect(pipe).toBeTruthy();
  });
});
